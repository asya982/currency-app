import { FC } from "react";
import styles from "./MainPage.module.css";

const MainPage: FC = () => {
  return <article className={styles.MainPage}>
    <h1>Currency</h1>
    <section>
        
    </section>
  </article>;
};

export default MainPage;
